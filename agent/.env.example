# Nebius Token Factory — the agents and the secondary render LLM use this.
# Create a key at https://tokenfactory.nebius.com/  (Settings -> API keys)
NEBIUS_API_KEY=...

# Optional overrides (sensible defaults baked into src/llm.py)
# NEBIUS_BASE_URL=https://api.tokenfactory.nebius.com/v1
# NEBIUS_MODEL=Qwen/Qwen2.5-72B-Instruct
